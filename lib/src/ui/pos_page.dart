import 'package:flutter/material.dart';

class PosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('My POS')),
      body: Center(child: Text('POS Main Screen')),
    );
  }
}
